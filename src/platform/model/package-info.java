/**
 * package contains components to build a platform model.
 *
 * For the platform we considered that there are clusters, which
 * contains Processors. Each Cluster also has a shared memory, and
 * DMA engines to communicate with other clusters / memory. It is connected
 * to the network via NetworkLink.
 * 
 * @author Pranav Tendulkar
 * 
 */
package platform.model;